{
  "name": "@cloudproof/serving",
  "version": "1.0.0",
  "private": true,
  "description": "Stateless embedding service: watches consensus for the model manifest, preloads into a shadow slot, activates by pointer swap",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "optionalDependencies": {
    "@aws-sdk/client-s3": "^3.600.0"
  }
}
